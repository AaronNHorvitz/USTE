[package]
name = "uste-txn"
description = "Durable commit coordination, coherent readers and idempotency for USTE"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
sha2 = { version = "=0.11.0", default-features = false }
uste-crypto = { path = "../uste-crypto", version = "=0.1.0" }
uste-storage = { path = "../uste-storage", version = "=0.1.0" }
uste-types = { path = "../uste-types", version = "=0.1.0" }

[lints]
workspace = true
