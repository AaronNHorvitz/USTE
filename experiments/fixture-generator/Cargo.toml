[package]
name = "uste-fixture-generator"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
blake3 = { version = "=1.8.7", default-features = false, features = ["std", "pure"] }

[lints.rust]
unsafe_code = "forbid"
